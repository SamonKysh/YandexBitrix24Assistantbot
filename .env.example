# === Telegram ===
TELEGRAM_BOT_TOKEN=________
ADMIN_TELEGRAM_IDS=________

# === PostgreSQL ===
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bitrix_bot_db
DB_USER=octagon
DB_PASSWORD=________

DATABASE_URL=sqlite:///bot.db

# === Yandex Cloud ===
YC_API_KEY=________________-
YC_FOLDER_ID=_____________

# === Парсер ===
PARSE_MAX_PAGES=5000
PARSE_DELAY_SECONDS=1.5

# === Гибридный поиск ===
USE_SEMANTIC_SEARCH = os.getenv("USE_SEMANTIC_SEARCH", "true").lower() == "true"

EMBEDDING_BATCH_SIZE = int(os.getenv("EMBEDDING_BATCH_SIZE", "16"))
EMBEDDING_CACHE_PATH = os.getenv("EMBEDDING_CACHE_PATH", "data/embeddings_cache.npz")